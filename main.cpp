
#include "FileReader.hpp"

int main() {

    std::filesystem::path file{"data/data.txt"};// object referencing the *location* a text file.

    FileReader reader;

    // TODO read the data.txt file and do something with it. I.e. print content.
    //  You may also try to split the content into a vector of individual lines (The character '\n' indicates a line break).

    // You can also use the reader to read other files on your PC.
}
